# OpenAI API Key (Required for AI suggestions)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Vercel Blob Storage (Auto-created by Vercel when you enable Blob Storage)
# BLOB_READ_WRITE_TOKEN=auto-generated-by-vercel

# Supabase (Optional - for database features)
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Custom AI Endpoint (Optional - defaults to /api/suggest)
# VITE_SUGGEST_URL=/api/suggest

# Optional backend URL for API requests (leave empty to use same origin)
VITE_API_BASE=

# Google Sheets Apps Script endpoint for form submissions
VITE_SHEETS_URL=https://script.google.com/macros/s/your-script-id/exec

# Chat safety/limits (optional)
CHAT_MAX_CHARS=1000
CHAT_RATE_WINDOW=1000
CHAT_MAX_REQUESTS=4
# leave empty to allow all languages (comma-separated codes like "en,tr,es")
CHAT_LANGS_WHITELIST=
